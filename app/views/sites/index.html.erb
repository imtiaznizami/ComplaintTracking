<h1>Listing sites</h1>

<table class="table table-striped table-bordered table-condensed">
  <tr>
    <th>Code</th>
    <th>Equipment vendor</th>
    <th>City</th>
    <th>Partner Status</th>
    <th>Partner Operator</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sites.each do |site| %>
  <tr>
    <td><%= site.code %></td>
    <td><%= site.equipment_vendor %></td>
    <td><%= site.address.city %></td>
    <td><%= site.partner.status %></td>
    <td><%= site.partner.operator %></td>
    <td><%= link_to 'Show', site %></td>
    <td><%= link_to 'Edit', edit_site_path(site) %></td>
    <td><%= link_to 'Destroy', site, confirm: 'All related sectors will be deleted as well. Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Site', new_site_path %>
